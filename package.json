{
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare",
    "test:unit-old": "vitest run --environment jsdom",
    "test": "vitest --run --reporter verbose --globals --environment jsdom --globals",
    "test:watch": "vitest --reporter verbose --globals",
    "prepare": "husky install",
    "lint": "eslint ."
  },
  "devDependencies": {
    "@nuxt/eslint-config": "^0.1.1",
    "@nuxt/test-utils-edge": "^3.0.0-rc.13-27781436.1f6b3be",
    "@vitejs/plugin-vue": "^3.2.0",
    "@vue/test-utils": "^2.2.1",
    "eslint": "^8.33.0",
    "husky": "^8.0.0",
    "jsdom": "^20.0.2",
    "nuxt": "3.0.0-rc.12",
    "unplugin-auto-import": "^0.12.1",
    "vite": "^3.2.2",
    "vitest": "^0.24.4"
  },
  "dependencies": {
    "vue": "^3.2.41"
  }
}
